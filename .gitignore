node_modules/
*.log
.DS_Store


# 文档（根目录 README.md / 开发指南.md 保留入库，其余 .md 忽略；skills/ 下的运行时技能文件是插件必需，放行）
*.md
!/README.md
!/开发指南.md
!skills/*.md
!skills/**/*.md

# Claude Code 本地配置/技能（不入项目仓库；开发用技能放全局 ~/.claude/skills/）
.claude/

# 临时检查脚本（下划线开头为 dev 专用，不入库；离线测试 test.mjs 入库以支持 CI，审计 §10）
**/_*.mjs

# 运行时产物
temp/
data/

# 表情包资源（用户 git clone 而来，体积大，不入库）
resources/stickers/

# 用户配置（含 apiKey 等敏感信息，不入库；仅 config/default_config 入库）
config/config.yaml
config/config.yaml.tmp

# 用户截图（不入库）
Screenshot_*.jpg
Screenshot_*.png

# 临时排查截图不入库
图*.jpg

# crawl4ai 运行时 venv（scripts/install-crawl4ai.sh 创建，数百 MB 不入库）
.crawl4ai-venv/

# 无损压缩原文归档（运行时数据，不入库）
data/context-archive/

# Python 编译缓存
__pycache__/
*.pyc
