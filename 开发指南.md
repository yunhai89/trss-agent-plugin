# 开发指南：技能（Skill）与工具（Tool）开发

> 本文档从 README 拆出并大幅扩充。**工具开发**部分是详细的 API 参考。
> 适用版本：对应仓库当前 master。所有 API 定义在 `model/toolkit/define.js`，公共出口 `model/toolkit/index.js`。

本插件有两种扩展方式，先分清概念：

| | 工具（Tool） | 技能（Skill） |
| --- | --- | --- |
| 本质 | 一个**动作**：模型可直接调用并执行 | 一段**说明书**：教模型何时/如何用工具 |
| 形态 | `.js`，`defineTool` 定义，有 `execute` | `SKILL.md`，纯指令，无 `execute` |
| 放置 | 插件根 `tools/` 目录 | 插件根 `skills/` 目录 |
| 加载 | 自动加载（启动时） | 自动加载 + `reload_skills` 工具热加载 |
| 例子 | `group_info`、`web_search`、`terminal` | `group-admin.md`、`deep-research.md` |

一句话：**工具增加"能做什么"，技能改善"什么时候做、怎么做"**。

---

## 一、技能开发（Skills）

### 1.1 什么是技能

技能 = **渐进式披露的"说明书"**（参考 OpenClaw AgentSkills 规范）。它**不新增动作**，只增强模型决策：告诉模型在某个场景下该用哪些工具、按什么顺序、有什么约束。

模型"主动调用"技能的机制（解决"AI 不调用 skill"）：

1. **目录始终可见**：所有技能的 `name` + `description` 编译成精简 `<available_skills>` 块，**每轮注入 system prompt**。模型由此知道有哪些技能、各自适用什么场景。
2. **按需加载正文**：任务匹配某技能描述时，模型调用内置 `skill` 工具，按名称加载该技能的完整说明并遵循——这就是"主动调用技能"的通道。
3. **兼容抢先注入**：`always: true` 正文常驻；`when` 关键词/正则命中时正文额外自动注入（向后兼容）。

### 1.2 编写一个技能

在插件根 `skills/` 目录新建 `my-skill.md`（自动加载；免重启可用 `reload_skills` 工具热加载）：

```markdown
---
name: my-skill
description: "一句话说清何时用：名词短语，是模型决定是否加载的唯一依据"
when: [关键词1, 关键词2]   # 可选：命中时抢先注入正文（向后兼容）
priority: 5               # 可选：目录/注入排序（数字越大越靠前）
always: false             # 可选：true 则正文常驻（慎用，占上下文）
---

# 技能正文

这里写完整的操作指引：用什么工具、按什么顺序、有哪些约束与最佳实践。
正文只在被加载后（目录命中描述 → 调用 skill 工具，或 when 命中）注入，所以可以写得详细。
可引用【运行能力盘点】等 perception 注入的数据。
```

### 1.3 frontmatter 字段

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `name` | ✅ | 技能唯一标识，模型用 `skill` 工具按它加载 |
| `description` | ✅ | **最关键**。目录里模型唯一的判定依据，写成"何时用"的名词短语 |
| `when` | ❌ | 关键词/正则数组，命中时抢先注入正文 |
| `priority` | ❌ | 排序权重 |
| `always` | ❌ | `true` 则正文常驻 system（占上下文，慎用） |

### 1.4 要点

- **`description` 是关键**——写成"何时用"的名词短语，别写成长篇流程（流程放正文）。
- 正文放决策约束与步骤，别只写"让用户去发某指令"。
- 技能不是工具：没有 `execute`，不能被直接执行，只能被加载为指令。

内置技能示例：`skills/group-admin.md`（群管）、`skills/deep-research.md`（深度研究）、`skills/capability-inquiry.md`（能力询问）、`skills/skillhub-install.md`（技能商店安装）。

---

## 二、工具开发（Tools）· 详细 API 参考

### 2.1 工具契约

一个工具就是一个对象（通常用 `defineTool` 包装）：

```js
{
  name: 'echo_group',                         // 必填：全局唯一标识（模型用它调用）
  description: '返回当前群名',                  // 必填：模型判断"何时用"的唯一依据，写清楚
  category: 'query',                          // 可选：RBAC 类别，决定权限门槛（见 2.7）
  parameters: { type: 'object', properties: {}, required: [] }, // JSON Schema，用 param.* 拼
  meta: { summary: '查群名', resultCap: 8000 }, // 可选：摘要/截断/审批等元数据（见 2.6）
  async execute(params, ctx) {                // 必填：执行逻辑，返回 string | object
    return '结果'
  },
}
```

### 2.2 快速开始

在插件根 `tools/` 目录新建 `.js`，自动加载（TRSS-Yunzai apps 风格）。**三种合法导出形态**：

```js
import { defineToolPack, defineTool, param, ok, getGroup } from '../model/toolkit/index.js'

// 形态 1（推荐）：工具包。name 作为命名空间，子工具自动加 `example__` 前缀防重名
export default defineToolPack({
  name: 'example',
  description: '我的工具包',
  tools: [
    defineTool({ name: 'ping', description: '...', category: 'query',
      parameters: param.object({}), async execute() { return ok('pong') } }),
  ],
})

// 形态 2：工具数组（匿名包，按文件名做命名空间）
// export default [ defineTool({...}), defineTool({...}) ]

// 形态 3：工厂函数（按 ctx 动态生成工具）
// export default function (ctx) { return [ defineTool({...}) ] }
```

`tools/example.js` 是带完整注释的模板，可直接改。

> **命名空间前缀**：`defineToolPack({ name: 'my' })` 会把子工具 `ping` 注册为 `my__ping`（除非 `prefix: false`）。模型看到的是带前缀的全名，调用时也用全名。

#### 目录型工具包（复杂逻辑拆分）

简单工具用单文件即可（`tools/foo.js`）。**复杂工具**（需 axios 请求、QQ 签名、多步处理等）推荐用**目录型**——把签名 / 请求 / 解析等拆成独立文件，入口 `import` 它们，类似云崽插件包：

```
tools/qqmusic/           # 目录名 = 包名
  index.js               # 入口（import sign.js, api.js → defineToolPack）
  sign.js                # QQ 签名算法（独立维护）
  api.js                 # axios 请求（独立维护）
  util.js                # 辅助函数
```

入口查找顺序：`index.js` / `index.mjs` / `同名.js`（如 `qqmusic.js`）。辅助文件由入口 `import`（Node ESM 自动），不会被当工具重复加载。

```js
// tools/qqmusic/index.js
import { defineToolPack } from '../../model/toolkit/define.js'
import { sign } from './sign.js'
import { search, getLyric } from './api.js'

export default defineToolPack({
  name: 'qqmusic',
  tools: [
    { name: 'search', execute: async (p) => search(sign(p)) },
    { name: 'lyric', execute: async (p) => getLyric(p.id) },
  ],
})
```

```js
// tools/qqmusic/sign.js —— 签名逻辑独立维护
export function sign(params) { /* QQ 签名算法 */ }
```

```js
// tools/qqmusic/api.js —— 请求逻辑独立维护
import axios from 'axios'   // 第三方库：需在插件根 package.json 加依赖（pnpm add axios）
export async function search(s) { return (await axios.get('...', { params: s })).data }
```

> 第三方库（axios / cheerio / crypto-js 等）在插件根 `package.json` 加依赖后可直接 `import`。加载失败的目录（无入口 / import 报错）记日志跳过，不影响其他工具。

### 2.3 SDK API 参考（`model/toolkit/index.js`）

#### `defineTool(spec)` → Tool

把一个工具 spec 规范化并校验。缺 `name`/`execute` 直接抛错（而不是运行时才暴雷）。

```js
defineTool({
  name: 'add_todo',                  // 必填
  description: '添加一条待办',         // 必填
  category: 'personal',              // 可选，默认 'query'
  parameters: param.object({         // 可选，缺省 { type:'object', properties:{} }
    text: param.str('待办内容'),
  }, ['text']),
  meta: { summary: '添加待办' },      // 可选
  async execute(p, ctx) {            // 必填
    // p = 模型传入的参数（已按 JSON Schema）
    // ctx = 运行时上下文（见 2.5）
    return ok({ id: 1, text: p.text })
  },
})
```

#### `defineToolPack(pack)` → Pack

定义工具包。`pack.name` 作命名空间，自动给子工具加 `${name}__` 前缀。

```js
defineToolPack({
  name: 'my',                  // 必填：命名空间
  description: '我的工具包',    // 可选
  author: 'your-name',         // 可选
  version: '1.0.0',            // 可选，默认 '1.0.0'
  prefix: true,                // 可选，默认 true；false 则不加前缀
  tools: [ defineTool({...}) ],           // 静态工具（与 factory 二选一）
  // factory: (ctx) => [ defineTool({...}) ], // 或按 ctx 动态生成
})
```

#### `param` —— 拼装 JSON Schema 的辅助

每项返回一个 JSON Schema 片段，省去手写样板。用 `param.object(properties, required)` 包成完整 schema。

| API | 签名 | 产出 |
| --- | --- | --- |
| `param.str(desc, opts?)` | `opts.enum` 可选 | `{ type:'string', description, enum? }` |
| `param.num(desc)` | | `{ type:'number', description }` |
| `param.int(desc, opts?)` | `opts.min` 可选 | `{ type:'integer', description, minimum? }` |
| `param.bool(desc)` | | `{ type:'boolean', description }` |
| `param.enum(desc, values)` | | `{ type:'string', description, enum:values }` |
| `param.object(properties, required=[])` | | `{ type:'object', properties, required }` |

```js
parameters: param.object({
  userId: param.str('目标 QQ 号'),
  duration: param.int('禁言秒数', { min: 0 }),
  scope: param.enum('范围', ['self', 'group']),
  enable: param.bool('是否开启'),
}, ['userId'])
```

#### ctx 访问辅助（优雅降级，缺失返回 `null`）

模型调用工具时，`execute(params, ctx)` 的 `ctx` 是运行时上下文对象（见 2.5）。这些辅助函数从 `ctx` 里安全地取出 Bot/群/好友/成员句柄，**协议端不支持时返回 `null` 而非报错**：

| API | 返回 | 说明 |
| --- | --- | --- |
| `getBot(ctx)` | Bot 句柄 \| null | `ctx.bot` 或全局 `Bot` |
| `getEvent(ctx)` | Yunzai 事件 \| null | `ctx.e` |
| `getGroup(ctx, groupId?)` | 群句柄 \| null | 优先 `ctx.e.group`，否则 `bot.pickGroup(gid)` |
| `getFriend(ctx, userId?)` | 好友句柄 \| null | 优先 `ctx.e.friend`，否则 `bot.pickFriend(uid)` |
| `getMember(ctx, groupId?, userId?)` | 成员句柄 \| null | 经 `getGroup` 再 `pickMember` |

```js
async execute(_p, ctx) {
  const g = getGroup(ctx)              // 当前群句柄（私聊为 null）
  if (!g?.getInfo) return ok('当前会话不支持查群信息')
  const info = await g.getInfo()
  return ok(`当前群：${info.group_name}`)
}
```

> 群/好友句柄的方法（`getInfo`/`getChatHistory`/`pickMember`/`kickMember`/`muteMember`/`setCard`…）来自协议端（icqq/napcat 等），**调用前用 `typeof x === 'function'` 特征检测**，缺失就降级返回，别假设一定存在。

#### 响应辅助

| API | 返回 | 用途 |
| --- | --- | --- |
| `ok(data, extra?)` | `{ ok:true, ...data, ...extra }` | 成功结果。`data` 为字符串时包成 `{ content: data }` |
| `fail(error, extra?)` | `{ ok:false, error, ...extra }` | 失败结果（`error` 可传字符串或 Error） |
| `markdown(text)` | `{ content:text, format:'markdown' }` | 返回 markdown 内容（按回复渲染） |
| `roleRank(role)` | `0~3` | 角色名 → 阶梯值（member0/admin1/owner2/master99） |

```js
return ok({ id: 1 })                 // 成功带数据
return ok('完成')                    // 成功纯文本（字符串自动包成 content）
return fail('未找到该成员')           // 失败（模型可见，可据此重试）
return markdown('# 标题\n正文')       // markdown
```

> 返回值最终会被 `JSON.stringify` 后喂给模型（字符串则原样）。**不需要手动 JSON 化**。

### 2.4 ToolRegistry（一般不需要直接用）

工具作者通常只写 `defineTool`/`defineToolPack` 放 `tools/`，加载由框架完成。了解底层注册器有助于调试：

```js
new ToolRegistry({ logger })
  .register(tool1, tool2)     // 变参：单个/多个/数组/嵌套数组均可
  .register(...array)
// 方法：list() / names() / get(name) / has(name) / match({names, predicate})
//      unregister(name) / toOpenaiTools() / toAnthropicTools()
```

注册时自动 AOP 包装 `execute`，统一打印"调用入参/耗时/结果/错误"日志（debug 级；MCP 工具 info 级），无论从哪条路径调用都可观测。

### 2.5 运行时 `ctx` 字段（`execute(params, ctx)` 的第二个参数）

`ctx` 是本次对话的运行时上下文，由 `apps/agent.js` 组装。常用字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `userId` | string | 调用者 QQ |
| `groupId` | string\|null | 群号（私聊为 null） |
| `isGroup` | bool | 是否群聊 |
| `role` | string | `'member'`/`'admin'`/`'owner'` |
| `isMaster` | bool | 是否主人 |
| `isGroupAdmin` | bool | 是否群管（管理员/群主） |
| `conversationId` | string\|null | 当前对话 id |
| `e` | object | Yunzai 原生事件（`e.reply`/`e.message`/`e.sender`…） |
| `bot` | object | Bot 句柄（`globalThis.Bot`） |
| `fetcher` | function | `fetch`（用于 HTTP 请求，`ctx.fetcher(url, opts)`） |
| `media` | array | 本次主动收集的附件（每项 `{ name, mime, buffer, bytes, source, url? }`） |
| `miyoushe` | object | `{ cookie, defaultGid }` |
| `terminal` | object | `{ cwd, maxTimeout, blocklist }` |
| `notify(id, info)` | function | 给 master 发审批通知 |

```js
// 私聊/群聊判断
if (!ctx.isGroup) return fail('该工具仅群聊可用')

// 取调用者
const who = ctx.userId

// 直接用 Yunzai 事件回复（少见，通常让模型回复而不是工具回复）
// await ctx.e.reply('通知')   // 慎用：会绕过模型的回复编排

// 发 HTTP
const res = await ctx.fetcher('https://api.example.com/x')
const data = await res.json()
```

> ⚠️ **工具不要主动 `e.reply` 给用户发消息**——回复由 Agent 统一编排。工具只返回数据，让模型基于数据组织回复。

### 媒体发送 API（`sendImage` / `sendVoice` / `sendVideo` / `sendFile` / `sendText`）

工具开发者在 `execute(params, ctx)` 里可**主动发送媒体**（图片/语音/视频/文件/文本）到聊天，而非只能返回文本给模型。从 `model/toolkit/index.js` 导入：

```js
import { defineTool, param, sendImage, sendText } from '../model/toolkit/index.js'

defineTool({
  name: 'send_chart',
  description: '生成图表并发送',
  category: 'query',
  parameters: param.object({ data: param.str('数据') }, ['data']),
  async execute(p, ctx) {
    const buf = generateChart(p.data)       // 你的图表生成逻辑
    await sendImage(ctx, buf)                // 直接发图给用户
    await sendText(ctx, '图表已生成上图')     // 可选：附带文字说明
    return { ok: true }                      // 返回给模型（模型知道已完成）
  },
})
```

| 函数 | 签名 | 说明 |
| --- | --- | --- |
| `sendImage(ctx, source)` | `(ctx, Buffer\|路径\|URL\|base64) → boolean` | 发送图片 |
| `sendVoice(ctx, source)` | 同上 | 发送语音（建议 mp3，NapCat/Lagrange 自动转 silk） |
| `sendVideo(ctx, source)` | 同上 | 发送视频 |
| `sendFile(ctx, source)` | `(ctx, 路径\|URL) → boolean` | 发送文件（走群文件/好友文件上传） |
| `sendText(ctx, text)` | `(ctx, string) → boolean` | 发送文本（中途播报/通知） |

**输入格式**（`source`）：`Buffer` 自动转 base64；本地路径、`http(s)://`、`base64://`、`file://` 原样透传（Yunzai segment 全接受）。成功返回 `true`，失败（segment 不可用 / reply 异常）返回 `false`。

> 注意：媒体发送是**即时**的（直接进聊天），不经过模型。发送后仍需 `return` 一个结果给模型（否则模型不知道工具完成了什么）。

### 2.6 `meta` 选项（控制工具行为）

`meta` 是工具对象上的可选字段，框架据它调整调度/审批/展示：

| 字段 | 类型 | 作用 |
| --- | --- | --- |
| `summary` | string | 工具目录里的速查摘要（system prompt 里每工具一行）；缺省截断 `description`。**写好它能让模型更准地选工具** |
| `resultCap` | number | 该工具结果字符上限（覆盖全局 `maxToolResultChars`）。内容获取类工具可设大（如 12000），管理类可设小 |
| `interactive` | bool | 批次内有此类工具时，**整批串行执行**（否则 `Promise.all` 并行）。状态/危险工具用它避免竞态 |
| `alwaysConfirm` | bool | 强制走主人 `#确认` 审批（即使 policy/master 放行）。**高危写操作用它** |
| `shouldConfirm(args, ctx)` | async\|sync | 按入参否决审批：返回 `false` 则**免审批直跑**（如 allowlist 只读命令命中）。仅 `alwaysConfirm`/policy 要求确认时才调 |
| `dangerous` | bool | 危险标记（标注用，如 terminal） |

示例——一个需审批的写操作工具：

```js
defineTool({
  name: 'set_something',
  description: '修改某配置（写操作，需主人确认）',
  category: 'system',
  meta: { alwaysConfirm: true, interactive: true, summary: '修改某配置' },
  parameters: param.object({ key: param.str('配置项'), val: param.str('值') }, ['key', 'val']),
  async execute(p, ctx) {
    // 走到这说明已被主人 #确认 放行
    return ok(`${p.key}=${p.val}`)
  },
})
```

示例——按入参否决审批（只读命令免审，写命令需审）：

```js
meta: {
  alwaysConfirm: true,
  async shouldConfirm(args, ctx) {
    // 只读安全命令 → false 免审直跑；否则照常需 #确认
    return !/^(ls|cat|grep|git status)$/.test(String(args.command || '').trim())
  },
}
```

### 2.7 `category` 与 RBAC 权限

`category` 决定**谁能用这个工具**。内置类别阶梯（数字 = 最低角色要求）：

| category | 最低角色 | 含义 | 例子 |
| --- | --- | --- | --- |
| `query` | 0（所有人） | 只读查询 | `web_search`、`group_info`、`get_chat_history` |
| `personal` | 0 | 个人数据 | `get_note`、`set_note` |
| `message` | 1（管理员+） | 涉及消息 | — |
| `group_manage` | 2（群管+） | 群管理写操作 | `group_set_card`、`group_members` |
| `system` | 3（主人/群主） | 高危/系统级 | `group_kick`、`group_mute`、`terminal` |

角色阶梯：`member(0) < admin(1) < owner(2) < master(99)`。

- 调用者角色 < 类别要求 → **policy 拒绝**（模型收到 `rejected_by_policy`）。
- 可在 config 用 `agent.policy.categoryMin` 覆盖内置门槛或加自定义类别：
  ```yaml
  policy:
    categoryMin:
      message: 1
      mcp_write: 2
  ```
- 高类别（`group_manage`/`system`）通常还会触发 `#确认` 审批门（可用 `meta.alwaysConfirm` 强制、`meta.shouldConfirm` 按入参放行）。

### 2.8 返回值、结果截断与错误处理

- **返回值**：`string` 原样；`object` 经 `JSON.stringify`。用 `ok/fail/markdown` 辅助产出结构化结果。
- **结果截断**：单条结果超过上限会被字符切片并附 `…(已截断，原文 N 字符)` 标记。上限 = `meta.resultCap` ?? 全局 `maxToolResultChars`（默认 8000）。**大结果工具（抓网页/读文件/拉历史）务必设 `meta.resultCap` 放宽，否则模型看到的是腰斩内容**。
- **错误处理**：
  - `throw` → 框架捕获，归一为 `{ error: message }` 喂回模型，**不中断循环**（模型可据 error 重试或换路）。日志 warn 级。
  - 返回 `fail(error)` 或 `{ error: '...' }` → 同样喂回模型，语义更清晰。
  - **别让工具抛未捕获异常导致整轮崩**：外部调用（HTTP/协议端）用 `try/catch` 包好，失败返回 `fail()`。

```js
async execute(p, ctx) {
  let info
  try {
    info = await ctx.fetcher(`https://api.x.com/${p.id}`).then(r => r.json())
  } catch (e) {
    return fail(`请求失败：${e.message}`)   // 模型可见，可重试
  }
  if (!info.ok) return fail('接口返回失败：' + info.msg)
  return ok(info.data)
}
```

### 2.9 完整示例

#### 例 1：查询类（只读，人人可用）

```js
import { defineToolPack, defineTool, param, ok, fail, getGroup } from '../model/toolkit/index.js'

export default defineToolPack({
  name: 'my',
  description: '我的工具包',
  tools: [
    defineTool({
      name: 'group_name',
      description: '返回当前群的群名。用户问"这是什么群/群名叫啥"时调用。',
      category: 'query',
      meta: { summary: '查当前群名' },
      parameters: param.object({}),
      async execute(_p, ctx) {
        const g = getGroup(ctx)
        if (!g?.getInfo) return fail('当前会话不支持查群信息')
        const info = await g.getInfo()
        return ok(info.group_name || '未知名')
      },
    }),
  ],
})
```

#### 例 2：带参工具 + HTTP

```js
defineTool({
  name: 'weather',
  description: '查询某城市天气。',
  category: 'query',
  meta: { summary: '查天气' },
  parameters: param.object({
    city: param.str('城市名，如 Beijing'),
  }, ['city']),
  async execute(p, ctx) {
    try {
      const r = await ctx.fetcher(`https://wttr.in/${encodeURIComponent(p.city)}?format=j1`)
      const j = await r.json()
      const cur = j.current_condition?.[0]
      return ok({ city: p.city, tempC: cur?.temp_C, desc: cur?.weatherDesc?.[0]?.value })
    } catch (e) { return fail(`天气查询失败：${e.message}`) }
  },
})
```

#### 例 3：群管理写操作（需群管 + 审批 + 串行）

```js
defineTool({
  name: 'mute_badword',
  description: '禁言某成员 10 分钟（触发违禁词）。',
  category: 'system',
  meta: { interactive: true, alwaysConfirm: true, summary: '违禁词禁言' },
  parameters: param.object({ userId: param.str('目标 QQ') }, ['userId']),
  async execute(p, ctx) {
    const g = getGroup(ctx)
    if (!g?.muteMember) return fail('协议端不支持禁言')
    await g.muteMember(String(p.userId), 600)   // 600 秒
    return ok(`已禁言 ${p.userId} 10 分钟`)
  },
})
```

#### 例 4：工厂模式（按 ctx 动态生成）

```js
export default defineToolPack({
  name: 'myfactory',
  factory: (ctx) => [
    defineTool({
      name: 'whoami',
      description: '返回调用者 QQ。',
      category: 'query',
      parameters: param.object({}),
      execute: async (_p, c) => ok(`你的 QQ：${c.userId}`),
    }),
  ],
})
```

### 2.10 加载、热加载与调试

- **自动加载**：启动时扫描 `tools/` 下 `.js`/`.mjs`（目录可由 `agent.tools.dir` 配置，默认 `tools`）。删除文件或改后缀即取消加载。
- **热加载**：工具改动需重启或 `#agents重载`（重建运行时）。技能可用 `reload_skills` 工具免重启热加载。
- **日志**：开启 `debug: true` 可看到每次工具调用的名称、入参、耗时、结果预览（`utils/Log.js`）。注册失败会打 `[toolkit] 注册失败`。
- **模块自检**：`node model/toolkit/test.mjs`（如有）跑离线断言。

### 2.11 常见陷阱

1. **重名**：工具 `name` 全局唯一。用 `defineToolPack` 的命名空间前缀避免冲突；**别和内置工具重名**（如 `web_search`），否则静默覆盖。
2. **`description` 写不清**：模型靠它判断何时调用。写成"做什么 + 何时用"，别只写名字。
3. **未检测协议端能力**：`g.kickMember`、`bot.pickMember` 等并非所有协议端都有，调用前 `typeof === 'function'` 检测，缺失返回 `fail()`。
4. **结果太大被截断**：大输出工具设 `meta.resultCap`，否则模型只能看到前 8000 字符。
5. **抛未捕获异常**：外部 IO（网络/文件/协议端）一律 `try/catch`，失败 `fail()`，别让整轮崩。
6. **工具里 `e.reply`**：不要主动给用户发消息，回复由 Agent 统一编排；工具只返回数据。
7. **`parameters` 不符 JSON Schema**：用 `param.*` 拼装，`required` 数组列必填项，模型才能正确填参。
8. **高危动作不走审批**：写/删/系统级操作务必 `category: 'system'` + `meta.alwaysConfirm: true`。

---

## 附：相关源码索引

| 关注点 | 位置 |
| --- | --- |
| SDK 定义（defineTool/param/ok/getGroup…） | `model/toolkit/define.js` |
| SDK 公共出口 | `model/toolkit/index.js` |
| 自动加载器 | `model/toolkit/loader.js` |
| 工具注册器（AOP 日志/并发） | `model/agent/tools/registry.js` |
| 工具执行 + 审批 + 截断门 | `model/agent/Agent.js`（`_executeOne`/`_executeToolCalls`/`_capToolResult`） |
| RBAC 策略 | `model/agent/policy.js` |
| 工具目录/速查 prompt | `model/prompt/library.js`（`buildToolCatalogSection`） |
| 内置工具范例 | `model/group/`、`model/media/tool.js`、`model/search/tools.js` |
| 工具包模板 | `tools/example.js` |
| 技能加载 | `model/skill/index.js` |
| 技能范例 | `skills/*.md` |
