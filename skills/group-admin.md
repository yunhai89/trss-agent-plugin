---
name: group-admin
description: "QQ 群管理操作：禁言/踢人/头衔/群名片/管理员/改群名，说明可用工具、权限分层与安全约束"
when: [禁言, 踢人, 踢出, 移除, 头衔, 群名片, 设管理员, 改群名, 全体禁言, 群管理, 管理员, group_kick, group_mute]
priority: 7
---

当用户想进行群管理操作时，按下列规范处理：

可用工具（参考【运行能力盘点】确认实际注册情况）：
- 读类：`group_info`（群信息）、`group_member`（某成员）、`group_members`（成员列表）
- 写类：`group_kick`（踢出）、`group_mute`（禁言 N 秒，0=解除）、`group_mute_all`（全体禁言）、`group_set_card`（群名片）、`group_set_title`（头衔）、`group_set_admin`（管理员）、`group_set_name`（改群名）

约束与最佳实践：
- **权限分层**：写类/敏感操作默认需要主人或群管权限，且危险动作会走审批（主人收到 DM 后 `#确认/#拒绝`）。先据【发言者】判断对方权限，不够就如实说明，不要尝试越权调用。
- **二次确认**：对不可逆操作（踢人、改群名、设管理员），调用前先用一句话向用户确认目标对象与动作，再执行。
- **安全护栏**：机器人不会对自身或操作者本人执行踢出/禁言；禁言上限 30 天。
- 出错（权限不足/协议端不支持）时，把返回的 error 透明转述给用户，并给替代建议。

语气：群管操作要稳，动作清晰、对象明确、结果如实反馈。
